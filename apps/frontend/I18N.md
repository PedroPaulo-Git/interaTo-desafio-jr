# Sistema de Internacionalização (i18n)

## 🌍 Idiomas Suportados

- **Português Brasileiro** (pt-BR) - Padrão
- **English** (en-US)

## 🚀 Como Usar

### No Código

```typescript
import { t } from '@/lib/i18n'

// Usar traduções
const title = t('dashboard.title') // "Dashboard" ou "Dashboard"
const loginButton = t('auth.login') // "Entrar" ou "Login"
```

### Trocar Idioma

```typescript
import { setLocale } from '@/lib/i18n'

// Trocar para inglês
setLocale('en-US')

// Trocar para português
setLocale('pt-BR')
```

### Componente de Seletor

Use o componente `LanguageSelector` no menu ou header:

```tsx
import { LanguageSelector } from '@/components/domain/language-selector'

function Header() {
  return (
    <header>
      {/* ... */}
      <LanguageSelector />
    </header>
  )
}
```

## 📝 Adicionar Novas Traduções

Edite `apps/frontend/src/lib/i18n.ts`:

```typescript
const translations = {
  'pt-BR': {
    'myFeature.title': 'Minha Funcionalidade',
    'myFeature.description': 'Descrição em português',
  },
  'en-US': {
    'myFeature.title': 'My Feature',
    'myFeature.description': 'Description in English',
  },
}
```

## 🔧 Arquitetura

- **Leve**: ~3KB de código, sem dependências externas
- **Type-Safe**: Todas as chaves são tipadas
- **Persistente**: Preferência salva no localStorage
- **SSR-Ready**: Funciona em server e client components

## 📦 Arquivos

- `src/lib/i18n.ts` - Core do sistema de traduções
- `src/components/domain/language-selector.tsx` - Componente UI
